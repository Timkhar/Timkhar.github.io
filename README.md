# Asset Vault — simulation demo

Static build of the stage-1 simulation (source: private repo Potentiacta-Asset-Vault-demo). Open https://timkhar.github.io/ — every visitor gets an independent in-browser simulation; the four wallets use public Anvil dev keys, the data package is public sample data.
